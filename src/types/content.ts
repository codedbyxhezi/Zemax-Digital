export type Service = {
  title: string;
  description: string;
  eyebrow: string;
};

export type Project = {
  title: string;
  category: string;
  description: string;
  metric: string;
};

export type ProcessStep = {
  step: string;
  title: string;
  description: string;
};

export type Testimonial = {
  quote: string;
  name: string;
  role: string;
};

export type Stat = {
  value: string;
  label: string;
};
